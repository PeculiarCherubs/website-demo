# Generated Event Media Placeholders

Six branded placeholder media cards were added under `assets/events/placeholders/`.

Their records are in `content/site-content.json > events > socialFeed` and are marked `placeholder: true`.

Replace their title, text, URL, and thumbnail when approved media becomes available. No API keys or account credentials are required.
